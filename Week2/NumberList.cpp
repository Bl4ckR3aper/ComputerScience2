//// Created by Arav Tuteja on 6/28/23.//#include <iostream>#include "/Users/arav/CLionProjects/ComputerScience2/Week2/NumberList.h"void NumberList::addNode(int data) {    // Create a new node    Node* newNode = new Node();    // Set the data of the new node to the given data    newNode->data = data;    // Make the new node point to the current head    newNode->next = head;    // Update the head to point to the new node    head = newNode;}void NumberList::printList() {    // Start from the head of the list    Node* current = head;    // Traverse the list and print the data of each node    while (current != nullptr) {        std::cout << current->data << " ";        current = current->next;    }    std::cout << std::endl;}void NumberList::mergeArray(double arr[], int size) {    // Iterate through the array    for (int i = 0; i < size; i++) {        // Add each element of the array as a new node to the front of the list        addNode(arr[i]);    }}void NumberList::displayList() {    // Simply call the printList function to display the elements of the list    printList();}NumberList::NumberList() {    // Initialize the head pointer to nullptr, indicating an empty list    head = nullptr;}